<?xml version="1.0" encoding="utf-8" ?>
    
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             xmlns:local ="clr-namespace:HydrationStationV1;assembly=HydrationStationV1"
             mc:Ignorable="d"
             x:Class="HydrationStationV1.MainPage"
            >

    <local:HomePage Title="Home"/>

    <ContentPage Title="Map">
        <StackLayout>
            <maps:Map x:Name="MainMap" MapType="Street" HasZoomEnabled="False" IsShowingUser="True">
                <maps:Map.ItemTemplate>
                    <DataTemplate>
                        <maps:Pin Position="{Binding Position}"
                          Address="{Binding Address}"
                          Label="{Binding PlaceName}"/>
                    </DataTemplate>
                </maps:Map.ItemTemplate>
            </maps:Map>
        </StackLayout>
    </ContentPage>

    <local:StatsPage Title="Stats"/>    
    
</TabbedPage>
